<li>
	{{burger_name}}

	<button type="button" class="btn btn-secondary change-devoured" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}Devoured!{{else}}EAT!{{/if}}
	</button>

	<button type="button" class="btn btn-danger delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
